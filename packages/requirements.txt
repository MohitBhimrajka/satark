# packages/requirements.txt
# ── Satark API Dependencies ──────────────────────────────────────────────────

# Core Framework
fastapi
uvicorn[standard]
gunicorn

# Database
sqlalchemy[asyncio]
psycopg2-binary
alembic

# AI — Gemini 3 SDK
google-genai

# Auth — JWT + password hashing
PyJWT==2.9.0
bcrypt
cryptography<44  # Pin: >=44 triggers SIGILL on aarch64 Docker (rust _openssl binding)

# File uploads (REQUIRED for FastAPI multipart)
python-multipart

# HTTP client (URL analysis + tests)
httpx

# PDF report generation
reportlab

# Cloud Storage
google-cloud-storage

# Settings management
pydantic-settings

# Email validation (required for Pydantic EmailStr)
email-validator

# Logging
python-json-logger

# ── Development & Code Quality ───────────────────────────────────────────────
black
isort
flake8

# ── Testing ──────────────────────────────────────────────────────────────────
pytest
pytest-asyncio
aiosqlite